# Environment variables (every strategy profile holds real API keys; only
# the .example templates are tracked)
.env
.env.*
!.env.example
!.env.*.example

# Strategy logs and runtime output
*.log
data/ndx_mom_buffer/

# Python caching
__pycache__/
*.pyc

# Local SQLite databases
data/*.db
